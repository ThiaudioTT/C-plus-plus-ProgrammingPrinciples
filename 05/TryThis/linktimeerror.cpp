#include "../../std_lib_facilities.h"


int area(int length, int width); // calculate area of a rectangle

int main()
{
    int x = area(2,3);
}

// undefined reference to `area(int, int)' collect2.exe: error: ld returned 1 exit status